.header {
    padding-top: 25px;

    display: flex;
    justify-content: space-between;
    align-items: center;
}

.header__menu {
    flex-grow: 1;

    @include tablets {
        display: none;
    }
}

.header__links {
    display: flex;
}